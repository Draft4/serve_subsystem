# tennis_serve_common 变更记录

## 0.7.0 — 2026-09-21

- 功能/接口：无 ROS 节点的场地坐标、角度和目标校验函数。当前代码按层级归档，本次补齐包 wiki。
- 算法和配置：`core.py` 把定位转换到 `court_origin_v2`，对绝对目标做边界校验，计算目标 yaw 与角度归一化。
- 测试结果：`test/test_aim.py` 间接覆盖朝向几何；当前包无独立测试目录。
- 已知限制：只负责几何与数值校验；定位新鲜度及机器人是否停止由调用方检查。
- Git commit/tag：本地初始化提交后记录；沿用现有包版本，未创建发布标签。
