# 工作空间变更记录

## 未发布 — 协作与部署说明

- 涉及包：`tennis_serve` 的集成文档。
- 在协作仓库根目录增加总 README，并说明开发阶段共用仓库、部署时按层级逐个迁移工作空间的规则。
- 修正板端构建及环境脚本的设备 underlay 路径，指向实际提供 `tennisbot_launcher` 的 `05_device_drivers/can_gateway_ws`。
- 验证：板端清洁构建 9 个包，33 项单元测试通过，关键模块与模型可加载；完整 ROS 图和实球流程未验证。
- 板端发现：俯仰状态接口暂无实测角度，Adapter 现以命令角度估算，发球门禁不校验俯仰反馈；已安装的 systemd 服务仍引用旧部署路径。
- Git commit/tag：本次文档提交；未创建发布标签，`VERSION` 保持 `0.7.0`。

## 0.7.0 — 2026-09-21

- 涉及包：`tennis_serve`。
- 当前代码从原单工作区按职责拆出；本次补齐两级 wiki、VERSION 和 Git 忽略规则。
- 验证：模型和核心逻辑已有本地测试；本次文档补齐后的完整板端运行**未验证**。
- 已知问题：负责人提交身份与统一 GitHub remote 待项目确认；板端定位、硬件和底盘接口需集成验证。
- Git commit/tag：协作仓库 `main` 的初始化提交；本次沿用包原有版本，未创建发布标签。远端备份状态以 `git status` 和远端分支核验。
